provider:
  selection: auto
  model: gpt-4.1
  openai_api_key_env: OPENAI_API_KEY
  openrouter_api_key_env: OPENROUTER_API_KEY

offline:
  provider: mock
  purpose: smoke tests and dry-run demonstrations
