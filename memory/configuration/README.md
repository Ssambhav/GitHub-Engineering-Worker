# Memory Configuration

Configuration templates define retention, sensitivity, checkpoint, compaction, and promotion policy defaults for future runtime code.

These files are declarative. They do not load, enforce, or execute policy.
