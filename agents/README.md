# Agents

This directory houses the autonomous agents responsible for specific tasks within the GitHub Issue lifecycle. Each agent is designed to be modular and focused on a particular aspect of the engineering workflow.

## Structure

Each agent typically has its own subdirectory containing its configuration, core logic, and any agent-specific tools or data.
