# Environment Configuration

Environment-specific configuration belongs here.

Use separate files for development, staging, and production values. Keep secrets in environment variables or a dedicated secret manager, not in this repository.
