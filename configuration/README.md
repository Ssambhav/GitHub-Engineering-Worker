# Configuration

This directory centralizes global project configuration files, settings, and credentials. It ensures that all components of the GitHub Engineering Worker can access necessary parameters in a consistent and secure manner.

## Contents

- `settings.yaml`: Global project settings (e.g., GitHub API tokens, database connection strings, external service endpoints).
- Environment-specific configuration files (e.g., development, staging, production).
