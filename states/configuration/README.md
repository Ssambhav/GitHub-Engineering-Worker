# State Configuration

Default policy knobs for the state machine.

Configuration values are declarative defaults only. Runtime policy resolution, environment overrides, and enforcement are separate future implementation concerns.
