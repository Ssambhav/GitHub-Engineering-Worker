# State Interfaces

Interface contracts for future state subsystem services.

These files define operation boundaries and expected inputs/outputs for state storage, transition validation, event publishing, and checkpoint management. They are not implementations.
