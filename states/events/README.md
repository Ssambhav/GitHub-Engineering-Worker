# State Events

Lifecycle event contracts for state entry, exit, transition attempts, pause/resume, recovery, cancellation, completion, and failure.

Events are immutable facts. Commands and requests should not be modeled as events.
