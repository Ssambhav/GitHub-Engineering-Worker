# State Definitions

Declarative catalogs for primary and nested execution states.

- `primary-states.yaml` defines the one-active-primary-state catalog.
- `nested-states.yaml` defines optional sub-state templates for long-running or side-effecting states.

Definitions must remain architecture contracts, not runtime code.
